-- new HOFs examples
-- -imm required

add(0,y)   = y
add(_x,_y) = succ(add(pred(x),y))

incr(_n) = @(/x add(x,1),n)

map([], f)    = []
map(x:xs, f)  = (@(f,x) : map(xs,f))
-- let [f1,f2] ~ !dup(f) in 
--app(_x,^f)     = ^f(x)

--comp(_x,^f,^g) = ^f(^g(x)) 